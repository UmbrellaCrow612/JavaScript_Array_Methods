/* toLocaleString and to locale string method */

var array = [1, 2, 3, 4];

console.log(array.toLocaleString("en-US", { style: "currency", currency: "USD" }));

/*

None mutative 

It turns the numbers or words etc specific to that country's locale representation of it 

This can be words, currency and more converted to that language specific


*/
