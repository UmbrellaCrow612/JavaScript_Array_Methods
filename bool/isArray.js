/* isArray method */

var isAnArray = [1, 2];

const isNotAnArray = "word";

console.log(Array.isArray(isAnArray));
console.log(Array.isArray(isNotAnArray));



/*

The args given to it will return a bool saying it the val passed 
is and array or not




*/
