/* forEach method */
var array = [1, 2, 3, 4, 5, 6];

console.log(
  array.forEach((item, index, array) =>
    console.log(item + " at index" + index + " array at " + array)
  )
);

/*

none mutative 



*/
