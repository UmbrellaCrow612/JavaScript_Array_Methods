/* Join method */

var array = [1, 2, 3, 4]

console.log(array.join("@"))


/*

None mutative 

return a stringified array, with the passed separator between elements  

A string used to separate one element of the 
array from the next in the resulting string. 
If omitted, the array elements are separated with a comma.

*/