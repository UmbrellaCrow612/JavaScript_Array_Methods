/* splice method */
var array = [1, 2, 3];

var deletedItems = array.splice(0, 2, "a", "b");

console.log(array);
console.log(deletedItems);

/*

mutative

Removes elements from an array and, if necessary, 
inserts new elements in their place, returning the deleted elements.

*/
