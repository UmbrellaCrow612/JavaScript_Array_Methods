/* filter method */
var array = [1, 2, 3, 4, 5, 6];

var ar = array.map((item) => item * 2);

console.log(ar);

/*

none mutative 

Calls a defined callback 
function on each element of an array, and returns 
an array that contains the results.

*/
